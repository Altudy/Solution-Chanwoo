List
====

A
```
- string변수 하나, int 변수하나를 쌍으로 갖고 입력에 따라 해당하는 int 1줄이고 출력 문자열길이m일때 O(m)
```

B
```

```

C
```
- 원소들을 정렬시키고 다시 처음부터 검사하게 되면 인접한 두 원소가 같은 때가 있는 지 O(N)만에 검사할 수 있다.
- 하지만 정렬하는데에 O(NlgN)이 소요되므로 총 TC는 O(NlgN).
```

D
```
- 먼저 각 원소에 해당하는 기대값을 double변수로 배열을 만들어 놓는다.
- 그 후 큐로 일정한 range의 합을 계속 검사하며 크기를 비교한다.
- 연산(N), 큐에 집어넣음(N) 큐에서 뺌(N)이므로 총 O(N)이다.
```

E
```
- DP로 풀 수 있겠다는 생각을 하지 못했던 문제.
- 앞에서부터 i개까지 들여다볼 때, 0<=j<=k의 숫자의 갯수를 가지면, 다음것을 고를때 뭘고르든 적용가능한 dp1과 기존 숫자에 딱걸리는 지점이 있는지 보는 dp0을 가지고 n사이즈까지 검사를 해나간다.
- n 사이즈를 모두 검사, 각 i에서 k개만큼 검사 그 안에서 또 0~9까지 처리를 하기 때문에 O(10*nk)가 걸리는데 Editorial에서는 구현에 따라 O(NK)의 구현도 있다고 하니 나중에 해보자.
```

F
```


```
